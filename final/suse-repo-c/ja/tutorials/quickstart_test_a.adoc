:doctype: book

= クイックスタート
:revdate: 2025-04-30
:page-revdate: {revdate}

image::single-cluster.png[]

ドキュメントなんて必要ない！

== インストール

{product_name} はヘルムチャートとして配布される。Helm 3はCLIで、サーバーサイドのコンポーネントはなく、使い方はいたって簡単だ。Helm 3 CLIをインストールするには、https://helm.sh/docs/intro/install[の公式インストール]手順に従ってください。

[CAUTION]
.{product_name} ランチャーにて
====
Rancherは{product_name} 、別のhelm chartを持っており、別のリポジトリを使っている。
====


[tabs]
====
Linux/Mac::
+
[source,bash]
----
brew install helm
helm repo add fleet https://rancher.github.io/fleet-helm-charts/
----

ウィンドウズ::
+
[source,bash]
----
choco install kubernetes-helm
helm repo add fleet https://rancher.github.io/fleet-helm-charts/
----
====

{product_name} Helmチャートをインストールする（究極の柔軟性のためにCRDを分けているため、2つある）。

[source,bash]
----
helm -n cattle-fleet-system install --create-namespace --wait fleet-crd \
    fleet/fleet-crd
helm -n cattle-fleet-system install --create-namespace --wait fleet \
    fleet/fleet
----

== ウォッチにGit Repoを追加する

`spec.repo` をお好みの git リポジトリに変更してください。 デプロイされるべきKubernetesマニフェストファイルは、リポジトリの`/manifests` 。

[source,bash]
----
cat > example.yaml << "EOF"
apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: sample
  # This namespace is special and auto-wired to deploy to the local cluster
  namespace: fleet-local
spec:
  # Everything from this repo will be run in this cluster. You trust me right?
  repo: "https://github.com/rancher/fleet-examples"
  paths:
  - simple
EOF

kubectl apply -f example.yaml
----

== ステータス取得

フリートが行っていることのステータスを得る

[source,shell]
----
kubectl -n fleet-local get fleet
----

クラスターにこのようなものが作成されるはずだ。

----
kubectl get deploy frontend
----

----
NAME       READY   UP-TO-DATE   AVAILABLE   AGE
frontend   3/3     3            3           116m
----

楽しんでxref:index.adoc[ドキュメントを]読んでください。
